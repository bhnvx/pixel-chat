{
  "name": "pixel-chat-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "deploy": "cd ../client && npm run build && cp -r dist/* ../server/public/ && cd ../server",
    "build": "tsc",
    "start": "node dist/index.js",
    "build:exe": "tsc && pkg dist/index.js --targets node18-win-x64 --output release/pixel-chat-server.exe --compress GZip"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "@yao-pkg/pkg": "^6.19.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
